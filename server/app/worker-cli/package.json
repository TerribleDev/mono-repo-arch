{
  "name": "worker-cli",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "build": "rm -rf dist && mkdir dist && cp -R *.ts dist/",
    "start": "ts-node dist/index.ts",
    "prestart": "yarn run build"
  },
  "dependencies": {
    "domain-worker": "1.0.0",
    "ts-node": "^9.1.1",
    "typescript": "^4.2.3"
  }
}
